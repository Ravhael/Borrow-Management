import React from 'react';

// This component was intentionally removed from the marketing profile page UI.
// Keeping a small stub in case other files reference this component in the future.
const MarketingProfilePerformance: React.FC<any> = () => null;

export default MarketingProfilePerformance;